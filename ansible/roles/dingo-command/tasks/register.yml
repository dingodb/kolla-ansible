---
- import_role:
    name: service-ks-register
  vars:
    service_ks_register_auth: "{{ openstack_dingo_command_auth }}"
    service_ks_register_services: "{{ dingo_command_ks_services }}"
    service_ks_register_users: "{{ dingo_command_ks_users }}"

